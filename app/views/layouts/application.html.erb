<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>El-Training</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="jumbotron jumbotron-fluid">
      <nav class="navbar navbar-expand-md navbar-light">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <% if logged_in? %>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active">
                <div class="nav-link">
                  <%= link_to image_tag('top.png', :size => '25x25', :alt => 'top page'), tasks_path(current_user.id) %>
                </div>
              </li>
              <li class="nav-item active">
                <div class="nav-link">
                  <%= link_to image_tag('person.png', :size => '25x25', :alt => 'personal page'), user_path(current_user.id) %>
                </div>
              </li>
              <li class="nav-item active">
                <a class="nav-link" href="/tasks/new">
                  <%= image_tag "pen.png", :size => "25x25", :alt => "task new page" %>
                </a>
              </li>
              <li class="nav-item active">
                <a class="nav-link" href="/labels/new">
                  <%= image_tag "label.png", :size => "25x25", :alt => "label new page" %>
                </a>
              </li>
              <li class="nav-item active">
                <div class="nav-link">
                    <%= link_to image_tag('logout.png', :size => '25x25', :alt => 'logout'), session_path(current_user.id), method: :delete %>
                </div>
              </li>
            </ul>
            <% else %>
            <ul class="navbar-nav">
              <li class="nav-item active">
                <div class="nav-link">
                  <%= link_to image_tag('login.png', :size => '25x25', :alt => 'login'), new_session_path %>
                </div>
              </li>
            </ul>
            <% end %>
          </ul>
        </div>
      </nav>

      <div class="container">
        <%= yield %>
      </div>

      <footer class="footer">
        <div class="container">
          <span>https://github.com/th3957/El-training</span>
        </div>
      </footer>
    </div>
  </body>
</html>
